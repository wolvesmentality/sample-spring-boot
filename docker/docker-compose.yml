version: '2'
services:
  postgres:
    image: postgres:9.6.13
    volumes:
    - postgres_volume:/var/lib/postgresql/data
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: book_rating
volumes:
  postgres_volume:
